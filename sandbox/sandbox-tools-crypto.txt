== CRYPTO (Sage) ==
sage                   Full SageMath — sage script.sage / sage -c '...'
sage-python            Sage's Python only (rarely needed)
sympy / pycryptodome   On system python3 (L0); pycryptodome also in sage when writable
gf128-roots            GF(2^128) roots (also on L0)
galois                 Optional — `pip3 install galois` if needed (heavy; pulls numba)

RsaCtfTool / cado-nfs / flatter / gmpy2 / fpylll load separately when first used.

== NOTES ==
`sage` and system `python3` are different interpreters — packages do not cross.
  Algebra: `sage` (pycryptodome works inside sage).
  Networking / `pwntools`: system `python3` only — `from pwn import *` fails under sage.
  Pattern: sage computes → write results; `python3` + pwntools (or sockets) for remote I/O.
Do not put /opt/sagemath/bin on PATH (it shadows python3).
Sage is shared read-only from the host pack cache across sandboxes.
Exit 137 → shrink memory work. Exit 124 → often network timeout; adjust before abandoning.
Re-read /challenge/TOOLS.txt after packs load.
